import React, { Component } from 'react';
import { AssetType } from '../../Asset/Asset';
export interface AssetCardPropTypes {
    src: string;
    title: string;
    extraClassNames?: string;
    isLoading?: boolean;
    dropdownListElements?: React.ReactNode;
    status?: 'archived' | 'changed' | 'draft' | 'published';
    testId?: string;
    type?: AssetType;
}
export interface AssetCardState {
    isOpen: boolean;
}
export declare class AssetCard extends Component<AssetCardPropTypes, AssetCardState> {
    static defaultProps: {
        isLoading: boolean;
        testId: string;
    };
    state: {
        isOpen: boolean;
    };
    renderDropdownListElements: (dropdownListElements: any) => JSX.Element;
    renderStatus: (status: any) => JSX.Element;
    render(): JSX.Element;
}
export default AssetCard;
